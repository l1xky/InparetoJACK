# Termux (Android) — use setup.sh only (do NOT plain pip install -r this on 3.13).

requests>=2.31,<3
urllib3>=2.0,<3
# Required on Termux for Posts count (chrome131_android TLS). setup.sh installs this.
curl_cffi>=0.7.0
# Install via Termux pkg (fast) — NOT pip on Py3.13:
#   pkg install -y python-cryptography python-pillow
typing-extensions>=4.14.1
annotated-types>=0.6.0
typing-inspection>=0.4.2
starlette>=0.40.0,<0.47.0
h11>=0.8
click>=7.0
fastapi>=0.110.0,<0.116.0
uvicorn>=0.29.0,<0.33.0
pydantic>=2.12.0,<2.13
